# 复杂技能分支清单

本目录专门存放后续“复杂技能分支待审阅清单”类文档。

使用约定：

1. 先从 `manual_skill_rule_reviews_all_20260612.json` 中筛选仍为 `partial` / `needs_review` 或带 `structure_gaps` 的技能。
2. 每批先生成待审阅文档，由用户确认规则、边界和实现结论。
3. 用户确认后再进入代码实现与规则写库。
4. 文档中的“建议实现方向”只作为实现讨论草案，不等于已写入正式规则。
